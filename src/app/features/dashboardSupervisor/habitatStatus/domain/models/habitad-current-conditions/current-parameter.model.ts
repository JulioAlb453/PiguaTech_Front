export interface CurrentParameter {
    //este modelo servira para obtener los valores
    //actuales como los valores de tendencia 
    // modificar si es necesario
    name: string;
    curretValue: number;
    unit: string;
    trend: number;
}
